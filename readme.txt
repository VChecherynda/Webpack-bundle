// Подключёны библиотеки React и Babel ( для работы с ES6);
// Когда скопирована сборка не забываем про 'npm i' ;

// npm run dev - запкуск watch ( отслеживает изменение в файлах );
// npm run prod - собираем финальный build ( всё автоматически удаляется из папки dist и в ней же собирается );

// файлы *.html - это templates.
// файлы *.scss - подключаются через app.js, contact.js и т.д. И потом подкл. при помощи соотв. плагинов в *.html .